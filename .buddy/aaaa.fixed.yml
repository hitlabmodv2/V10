- pipeline: wilykun
  name: aaaa
  refs:
  - refs/heads/main
  events:
  - type: PUSH
  fail_on_prepare_env_warning: true
  actions:
  - action: npm install pm2 -g && pm2 start index.js && pm2 save && pm2 monit
    type: BUILD
    docker_image_name: library/node
    docker_image_tag: 22
    execute_commands:
    - "# yarn install"
    - npm install pm2 -g && pm2 start index.js && pm2 save && pm2 monit
    shell: BASH
